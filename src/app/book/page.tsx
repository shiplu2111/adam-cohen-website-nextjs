import BookPageClient from "./_components/BookPageClient";

export const metadata = {
  title: "Book a Strategy Session | Adam Cohen",
  description: "Reserve your high-stakes strategy session with Adam Cohen to audit your current trajectory and engineer absolute market mastery.",
};

export default function Book() {
  return <BookPageClient />;
}
